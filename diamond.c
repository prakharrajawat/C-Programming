#include <stdio.h>
patternDiamond(int n);
int main()
{
  printf("enter no. of rows :: ");
  scanf("%d", &n);
}

patternDiamond(int n)
{
  for (int rows = 0; rows <= n; rows++)
  {
    int totalColumns =
        int numberOfSpaces = totalColumns -
                             for (int spaces)

                                 for (int col = 0; col <= totalColumns; col++)
    {
      printf("* ");
    }
    printf("\n");
  }
}
