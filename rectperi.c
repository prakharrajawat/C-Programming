#include <stdio.h>

void main()
{

  int l, b, p;

  printf("enter length:");
  scanf("%d", &l);

  printf("enter breath:");
  scanf("%d", &b);

  p = 2 * (l + b);
  printf("value of 4power 5 is %d", 4 ^ 5);
  printf("perimeter is:%d", p);
}